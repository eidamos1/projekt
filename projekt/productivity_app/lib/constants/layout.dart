abstract final class Layout {
  static const double maxContentWidth = 700;
  static const double sidebarWidth = 280;
  static const double wideScreenThreshold = 1000;
}

enum LayoutMode { compact, spread }
